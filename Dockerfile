# استخدم صورة PHP الرسمية
FROM php:8.2-cli

# إعداد مجلد العمل
WORKDIR /var/www/html

# نسخ كل الملفات إلى الحاوية
COPY . .

# تشغيل خادم PHP الداخلي على المنفذ 10000
CMD ["php", "-S", "0.0.0.0:10000", "-t", "."]
